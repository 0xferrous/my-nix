- whenever editing Neovim config, always ensure every user-facing keymap has a `desc`, including keymaps defined through helper wrappers around `vim.keymap.set` / `vim.api.nvim_set_keymap`
- whenever editing Neovim which-key configuration, keep groups aligned with actual active mappings: remove stale groups, add missing prefix groups for used keyspaces, and label groups semantically
- whenever editing Neovim keymaps or plugin key bindings, update `KEYMAPS.md` by auditing the config with code search and code-aware review; do not rely on a static generator as the sole source of truth. `KEYMAPS.md` should include separate sections for keymaps defined in this repo, keymaps provided by dependency/downstream plugins that are active in this setup, and trigger-only/plugin-default entries. When completeness matters, also inspect dependency plugins used by the config, and prefer reusing prior indexing work for unchanged dependency plugin versions/revisions instead of re-auditing them from scratch
- in Neovim keymap documentation, explicitly note when a related plugin or feature is always disabled, so the docs distinguish active mappings from unavailable ones
