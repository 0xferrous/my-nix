# my-nix

Public Nix files extracted from my larger setup.

Current public building blocks include:

- `pkgs/frs-nvim`: portable Neovim wrapper config exposed via this repo's flake `packages` and `apps`
