# The virtie agent is reached as agent@vsock/<CID>. It is a trusted local VM,
# so permit this SSH-kitten connection to control the originating Kitty instance.
hostname agent@vsock/*
forward_remote_control yes
